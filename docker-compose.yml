services:
  web:
    build: .
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - API_INTERNAL_URL=http://host.docker.internal:9090
      - NEXT_PUBLIC_API_URL
